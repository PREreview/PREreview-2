type ExternalTeam {
  id: ID!
  manuscriptId: ID!
  members: [ExternalUser!]!
  teamType: String!
}

extend type Query {
  getExternalTeamsForManuscript(manuscriptId: ID!): [ExternalTeam]
}

extend type Mutation {
  inviteExternalReviewer(manuscriptId: ID!, reviewerId: ID!): ID!
}
